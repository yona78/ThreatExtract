# ThreatExtract runtime dependencies (pinned for reproducibility).
# torch is the CPU build: on arm64 (e.g. Apple Silicon) the default wheel is
# already CPU-only, and on x86_64 the Dockerfile installs it from the PyTorch
# CPU index to slim the image. This file stays platform-portable for local dev.

streamlit==1.40.0
transformers==4.46.3
torch==2.5.1
huggingface_hub==0.26.2
tokenizers==0.20.1
sentencepiece==0.2.0
python-magic==0.4.27
pandas==2.2.3
psutil==6.1.1
